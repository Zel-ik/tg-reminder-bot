version: "3.9"

services:
  bot:
    build: .
    container_name: telegram_bot
    restart: always
    depends_on:
      - db
    env_file:
      - .env
    networks:
      - bot_network

  db:
    image: postgres:15
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: VXfAcVrGMzpbNyrjWVVGkLdxjDiLoTIm
      POSTGRES_DB: postgres.railway.internal
    ports:
      - "5432" # Это проброс порта, если нужен внешний доступ
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql # Сценарий инициализации БД
    networks:
      - bot_network

networks:
  bot_network:


volumes:
  pg_data:
